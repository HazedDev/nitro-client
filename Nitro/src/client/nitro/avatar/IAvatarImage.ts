export interface IAvatarImage {
    _Str_867(CHEST: string);
    _Str_784(): any;
    setDirection(FULL: string, direction: number);
    dispose();
    isPlaceholder();
    _Str_677: boolean;
    _Str_833();
    _Str_953(arg0: number);
    _Str_781();
    getImage(FULL: string, highlightEnabled: boolean, args?: number);
    _Str_899(): boolean;
    getDirection();
    _Str_754();
    _Str_607(spriteData: any);
    _Str_797();
    getAsset(assetName: string);
    _Str_880(FULL: string, direction: number);
    _Str_913();
    appendAction(POSTURE: string, _posture?: string|number, _postureParameter?: string): void;
    _Str_962();
}